<!-- REVIEW how to pull image from url? how to generate an image? -->
<!-- TODO additional styling, resizing -->

<% topic.bookmarks.each do |bookmark| %>
<div class="image-wrapper">
  <!-- TODO user Faker::Placeholdit https://github.com/stympy/faker#fakerplaceholdit-->
  <%= link_to image_tag("http://driph.com/words/wp-content/uploads/2008/04/e.gif", size: "120x120"), bookmark.url  %>
  <!-- TODO change this to link to "like" function -->
  <span class="caption">LIKE</span>

  <!-- <%# link_to edit_topic_bookmark_path(topic, bookmark), method: :edit, class: 'btn btn-primary' do %> -->
  <%= link_to edit_topic_bookmark_path(topic, bookmark), class: 'btn btn-primary' do %>
    <i class="glyphicon glyphicon-edit"></i>
  <% end %>

  <%# link_to 'Delete', wiki_path(@wiki), method: :delete, class: "btn btn-danger " %>

  <!-- REVIEW why is the hover on edit/delete buttons inconsistent? -->
  <!-- TODO style to make this center underneath LIKE link -->
  <!-- TODO smaller icons? -->
  <%= link_to topic_bookmark_path(topic, bookmark), method: :delete, class: "btn btn-danger " do %>
    <i class="glyphicon glyphicon-trash"> </i>
  <% end %>



  <!-- <%# link_to topic_bookmark_path(bookmark), method: :delete, class: 'btn btn-danger' do %> -->
  <!-- <%# link_to [topic, bookmark], method: :delete, class: "btn btn-danger" do%> -->
  <!-- <%# link_to 'Delete', [topic, bookmark], method: :delete, class: "btn btn-danger" do %> -->
  <%# link_to content_tag(:i, "", class: "icon-trash icon-white") + "Delete", topic_bookmark_path(topic, bookmark), method: :delete, class: "btn btn-danger remove_fields" %>
  <%# link_to topic_bookmark_path(topic, bookmark), method: :delete, class: "btn btn-danger" do %>
    <!-- <i class="glyphicon glyphicon-trash"> </i> -->
  <%# end %>

  <!-- glyphicon glyphicon-edit -->
  <!-- glyphicon glyphicon-trash -->

</div>
<% end  %>
